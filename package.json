{
  "private": true,
  "name": "freezable",
  "version": "1.1.4",
  "main": "dist/index.js",
  "types": "types/index.d.ts",
  "description": "Provides the freezable interface.",
  "keywords": ["freezable", "object"],
  "author": "retueze",
  "repository": {
    "type": "git",
    "url": "https://github.com/retueZe/node-freezable"
  },
  "scripts": {
    "lint": "eslint",
    "buildTsDev": "tsc -p tsconfig.dev.json",
    "buildDev": "npm run buildTsDev",
    "buildTsProd": "tsc -p tsconfig.prod.json",
    "buildProd": "npm run buildTsProd",
    "prepare": "rd /s /q dist && rd /s /q types && npm run buildProd"
  },
  "devDependencies": {
    "@types/node": "^16.3.1",
    "@typescript-eslint/parser": "^4.28.3",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "composite-types": "^1.0.0"
  }
}
